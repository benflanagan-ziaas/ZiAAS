interface:
  display_name: "PowerShell Static Gate"
  short_description: "Mandatory final static analysis for PowerShell"
  default_prompt: "Use $powershell-static-gate to validate every PowerShell script before handing it off."
policy:
  allow_implicit_invocation: true
